<div class="container bg-white">
  <div class="row p-3">
    <div class="col-12 py-3 rounded mb-3 bg-white">
      <h3>Welcome to Blogspot</h3>
      <p>Here you can read tons of blogs to increase your knowledge. </p>
      <p>You can also write your own blogs.</p>
    </div>
  </div>
  <%= image_tag(current_user.profile_img) if current_user.profile_img.attached? %>
  <div class="row">
    <div class="col-12 mx-auto">
      <%= render @posts if !@posts.nil? %>
    </div>
    <div class="col-12 mx-auto bg-white">
      <%= paginate @posts if !@posts.nil? %>
    </div>
  </div>
</div>
