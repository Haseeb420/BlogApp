<div class="row px-3 bg-white">
  <h2 class="mb-3 p-4">Posts </h2>
  <hr>
</div>
<div class="row bg-white px-3">
  <h3>Recent Posts</h3>
  <hr>
  <table class="table  table-striped">
    <thead>
      <tr>
        <th scope="col">Suggestion#</th>
        <th  scope="col">Sugesstion Text Title</th>
        <th scope="col">Suggest ON Post</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody id="post_data">
      <% @suggestions.each do |suggestion| %>
        <tr>
          <th scope="row"><%= suggestion.id %>
          </th>
          <td><%= suggestion.body %></td>
          <td id="suggestion-<%= suggestion.id%>-status" ><%= suggestion.post_id %></td>
          <td>
            <%= link_to "Delete", delete_suggestions_path(id:suggestion.id), class:"btn mx-auto btn-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
